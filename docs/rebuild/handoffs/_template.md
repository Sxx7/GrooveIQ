# Hand-off: Session NN — {name}

> Fill this in at the **end** of your session, before committing. Each line is a contract with the next session.

## Status
- [ ] Session goal achieved
- [ ] Visual verification done (preview screenshot attached or path noted)
- [ ] No regressions in old `/dashboard`
- [ ] Committed on `gui-rebuild` branch with message `rebuild: session NN — {name}`

## What landed

One-paragraph summary of what files / features / behaviour now exist that didn't before.

## File inventory after this session

List of files created or substantially modified. Format: `path/to/file — what it does now`.

## State of the dashboard at end of session

What's working at `/static/dashboard-v2.html`? What's broken or stubbed? Be specific.

## Decisions made (with reasoning)

If you made a non-obvious design or implementation decision (e.g. "used SVG instead of canvas for the area chart because…"), record it here. The next session inherits these decisions.

## Gotchas for the next session

Things that surprised you. Endpoints that returned unexpected shapes. CSS edge cases. State management subtleties. Anything that could trip up the next session.

## Open issues / TODOs

If you finished the session goal but hit something out-of-scope, list it here. Don't fix it now. Examples: "noticed `/v1/downloads/stats` returns null for backends with zero history — handled gracefully, but worth noting for the Downloads telemetry session".

## Verification screenshots

Paste paths to screenshots saved during preview verification. (Use `preview_screenshot` and save to a known path.)

## Time spent

Rough estimate. Helps calibrate future-session sizing.

---

**For the next session to read:** {next session number and name}, see `docs/rebuild/{NN}-{slug}.md`.
